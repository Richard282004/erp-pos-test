# Secretos / entorno
.env
*.env
.env.*
*.bak
!.env.example
# La URL de la API no es secreta: Vite la hornea en el bundle publico.
!Frontend/.env.production

# Python
__pycache__/
*.py[cod]
.venv/
venv/
Backend/venv/
Backend/.venv/

# Node / Vite
node_modules/
Frontend/dist/
Frontend/node_modules/
*.local

# Sistema / editor
.DS_Store
.idea/
.vscode/
.claude/settings.local.json

# Build del frontend (Vercel lo genera solo)
Frontend/dist/
schema-local.sql

# Backups / dumps de datos (NO el schema.sql que sí va al repo)
backup_*.sql
backup_*.dump
*.dump
Backend/backups/

# Wrangler (Cloudflare) local state
.wrangler/
